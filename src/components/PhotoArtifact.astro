<div class="artifact photo-artifact desktop-object" aria-label="Photo collection">
  {[0,1,2].map(index => (
    <a class="photo-frame" href="/life" data-scene="life" data-signature="photo" style={`--card: ${index}`} aria-label={`Photo frame 00${index + 1} — Life scene`}>
      <span class="photo-placeholder" aria-hidden="true"></span><span class="photo-number">00{index + 1}</span>
    </a>
  ))}
</div>
