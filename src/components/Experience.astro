---
import BaseLayout from '../layouts/BaseLayout.astro';
import SiteHeader from './SiteHeader.astro';
import LetterHero from './LetterHero.astro';
import SceneShell from './SceneShell.astro';
import DotPointer from './DotPointer.astro';
import type { Scene } from '../content/scenes';
interface Props { scene?: Scene }
const { scene } = Astro.props;
---
<BaseLayout title={scene ? `THISHI — ${scene.title}` : undefined}>
<div class="landing" data-experience data-scene={scene?.id ?? 'home'}>
<SiteHeader />
<main id="main" tabindex="-1">
<div data-home hidden={!!scene}><LetterHero /></div>
<SceneShell scene={scene} />
</main>
<span class="scroll-cue metadata" data-scroll-cue hidden={!!scene}>SCROLL ↓</span>
</div>
<div class="transition-layer" data-transition-layer aria-hidden="true"></div>
<DotPointer />
<script>import '../scripts/home';</script>
</BaseLayout>

